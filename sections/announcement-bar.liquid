{%- comment -%}
  Mend & Co — Promo bar (ledger marquee)
  Replaces the flash-sale countdown with brand-appropriate scrolling
  entries: journal posts, restocks, ritual notes. Each item has a
  short "mark" (N° XX, ✶, ◐) + text + optional link.
{%- endcomment -%}

{%- if section.settings.show -%}
  {%- assign items = section.blocks -%}

  {%- comment -%}
    Default items when no blocks are configured in the theme editor.
    Distilled 2026-05-05 from 5 entries to 2 — concrete value beats poetry.
  {%- endcomment -%}
  {%- assign default_marks = '◐|—|✶' | split: '|' -%}
  {%- assign default_texts = 'Built on hardware shipped to 30,000+ wearers worldwide|Free 30-day returns|No subscription — own your data outright' | split: '|' -%}

  {%- assign scroll_speed = section.settings.speed | default: 60 -%}
  <div class="promo-bar" role="region" aria-label="Announcements">
    <div class="marquee promo-bar__marquee" style="animation-duration: {{ scroll_speed }}s;">
      {%- for cycle in (1..2) -%}
        {%- if items.size > 0 -%}
          {%- for block in items -%}
            <span class="promo-bar__item" {{ block.shopify_attributes }}>
              <span class="promo-bar__mark">{{ block.settings.mark | default: '✶' }}</span>
              <span class="promo-bar__text">{{ block.settings.text | escape }}</span>
              {%- if block.settings.link_url != blank and block.settings.link_label != blank -%}
                <span class="promo-bar__sep" aria-hidden="true">·</span>
                <a class="promo-bar__link" href="{{ block.settings.link_url }}">{{ block.settings.link_label }}</a>
              {%- endif -%}
            </span>
          {%- endfor -%}
        {%- else -%}
          {%- assign default_count = default_texts.size | minus: 1 -%}
          {%- for i in (0..default_count) -%}
            <span class="promo-bar__item">
              <span class="promo-bar__mark">{{ default_marks[i] }}</span>
              <span class="promo-bar__text">{{ default_texts[i] }}</span>
            </span>
          {%- endfor -%}
        {%- endif -%}
      {%- endfor -%}
    </div>
  </div>
{%- endif -%}

<style>
  .promo-bar {
    background: var(--gold);
    border-bottom: 1px solid rgba(107, 63, 160, 0.4);
    height: 36px;
    overflow: hidden;
    position: relative;
    display: flex;
    align-items: center;
    width: 100%;
    box-sizing: border-box;
    contain: layout paint;
    font-family: var(--font-body);
    font-size: 12px;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--color-text-on-dark);
  }

  .promo-bar__item {
    display: inline-flex;
    align-items: center;
    gap: 12px;
    white-space: nowrap;
  }

  .promo-bar__mark {
    font-family: var(--font-body);
    font-style: italic;
    font-size: 15px;
    color: var(--color-cta-hover);
    letter-spacing: 0.04em;
  }

  .promo-bar__sep { opacity: 0.4; }

  .promo-bar__link {
    color: var(--color-cta-hover);
    text-decoration: underline;
    text-underline-offset: 3px;
    text-decoration-color: rgba(124, 58, 237, 0.45);
    transition: text-decoration-color 0.15s ease;
  }
  .promo-bar__link:hover { text-decoration-color: var(--color-cta-hover); }
</style>

{% schema %}
{
  "name": "Promo bar",
  "settings": [
    {
      "type": "checkbox",
      "id": "show",
      "label": "Show promo bar",
      "default": true
    },
    {
      "type": "range",
      "id": "speed",
      "label": "Scroll duration (seconds — lower is faster)",
      "min": 15,
      "max": 120,
      "step": 5,
      "unit": "s",
      "default": 60
    },
    {
      "type": "paragraph",
      "content": "Brand-appropriate ledger marquee. Add 3–6 items (journal entries, restocks, ritual notes). They scroll continuously."
    }
  ],
  "blocks": [
    {
      "type": "item",
      "name": "Marquee item",
      "settings": [
        {
          "type": "text",
          "id": "mark",
          "label": "Mark (e.g. N° 04, ✶, ◐)",
          "default": "✶"
        },
        {
          "type": "text",
          "id": "text",
          "label": "Text",
          "default": "Restocked — Restorative Elixir, 30ml"
        },
        {
          "type": "text",
          "id": "link_label",
          "label": "Link label",
          "default": "read"
        },
        {
          "type": "url",
          "id": "link_url",
          "label": "Link URL"
        }
      ]
    }
  ],
  "max_blocks": 6,
  "presets": [
    {
      "name": "Promo bar"
    }
  ]
}
{% endschema %}
