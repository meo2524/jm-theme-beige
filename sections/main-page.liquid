<!-- STUB — full build coming next session -->
<section class="section">
  <div class="container container--narrow">
    <h1 class="t-display-md">{{ page.title }}</h1>
    <div class="page__content" style="margin-top:2rem;">{{ page.content }}</div>
  </div>
</section>

{% schema %}
{
  "name": "Page",
  "settings": [],
  "presets": [{ "name": "Default page" }]
}
{% endschema %}
