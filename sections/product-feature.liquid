{%- liquid
  assign eyebrow  = section.settings.eyebrow   | default: 'The science'
  assign heading  = section.settings.heading   | default: 'Most routines start too late.'
  assign body     = section.settings.body      | default: 'Most people don’t know what their body is doing while they sleep. The Smart Band tracks HRV, recovery, and sleep architecture overnight, then tells you in the morning whether to push or rest.'
  assign cta_label = section.settings.cta_label | default: 'Learn more'
  assign cta_url   = section.settings.cta_url
  assign image     = section.settings.image
  assign video_url = section.settings.video_url
  assign reverse   = section.settings.reverse
-%}

<section class="product-feature section">
  <div class="container">

    <div class="product-feature__grid{% if reverse %} product-feature__grid--reverse{% endif %}">

      <!-- Media (video takes priority over image) -->
      {%- assign video_y = section.settings.video_position_y | default: 50 -%}
      <div class="product-feature__media">
        {%- if video_url != blank -%}
          <video
            src="{{ video_url }}"
            {%- if image != blank %}
            poster="{{ image | image_url: width: 1200 }}"
            {%- endif %}
            autoplay
            muted
            loop
            playsinline
            preload="metadata"
            class="product-feature__video"
            style="object-position: 50% {{ video_y }}%;"
            aria-label="{{ heading | escape }}"
          >
            {%- if image != blank -%}
              <img
                src="{{ image | image_url: width: 1200 }}"
                alt="{{ image.alt | default: heading | escape }}"
                width="1200"
                height="1467"
                loading="lazy"
              >
            {%- endif -%}
          </video>
        {%- elsif image != blank -%}
          <img
            src="{{ image | image_url: width: 900 }}"
            srcset="
              {{ image | image_url: width: 600 }} 600w,
              {{ image | image_url: width: 900 }} 900w,
              {{ image | image_url: width: 1200 }} 1200w
            "
            sizes="(max-width: 767px) 100vw, 50vw"
            alt="{{ image.alt | default: heading | escape }}"
            width="900"
            height="1100"
            loading="lazy"
          >
        {%- else -%}
          <div class="product-feature__media-placeholder" aria-hidden="true"></div>
        {%- endif -%}
      </div>

      <!-- Copy -->
      <div class="product-feature__copy">

        <span class="rule-above" aria-hidden="true"></span>

        {%- if eyebrow != blank -%}
          <p class="product-feature__eyebrow t-label" data-reveal data-reveal-delay="0">{{ eyebrow }}</p>
        {%- endif -%}

        <h2 class="product-feature__heading t-display-md" data-reveal data-reveal-delay="80">{{ heading }}</h2>

        {%- if body != blank -%}
          <div class="product-feature__body" data-reveal data-reveal-delay="160">
            <p>{{ body }}</p>
          </div>
        {%- endif -%}

        <!-- Optional bullet points -->
        {%- if section.blocks.size > 0 -%}
          <ul class="product-feature__bullets" role="list">
            {%- for block in section.blocks -%}
              {%- if block.type == 'bullet' -%}
                <li class="product-feature__bullet" {{ block.shopify_attributes }}>
                  <span class="product-feature__bullet-icon" aria-hidden="true">—</span>
                  <span>{{ block.settings.text }}</span>
                </li>
              {%- endif -%}
            {%- endfor -%}
          </ul>
        {%- endif -%}

        {%- if cta_url != blank and cta_label != blank -%}
          <a href="{{ cta_url }}" class="btn btn--secondary" style="margin-top: var(--space-8);" data-reveal data-reveal-delay="160">
            {{ cta_label }}
          </a>
        {%- endif -%}

      </div>
      <!-- /product-feature__copy -->

    </div>

  </div>
</section>

<style>
  .product-feature__grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-16);
    align-items: center;
  }

  .product-feature__grid--reverse .product-feature__media {
    order: 2;
  }

  .product-feature__grid--reverse .product-feature__copy {
    order: 1;
  }

  /* ---- Media ---- */
  .product-feature__media {
    position: relative;
    aspect-ratio: 9 / 11;
    overflow: hidden;
    border-radius: var(--radius-base);
    background-color: var(--color-bone-alt);
  }

  .product-feature__media img,
  .product-feature__video {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    display: block;
  }

  .product-feature__media-placeholder {
    width: 100%;
    height: 100%;
    background-color: var(--color-bone-alt);
    border: 1px dashed rgba(166, 137, 102, 0.2);
  }

  /* ---- Copy ---- */
  .product-feature__copy {
    display: flex;
    flex-direction: column;
    gap: 0;
    padding-block: var(--space-8);
  }

  .product-feature__eyebrow {
    color: var(--color-walnut);
    margin-bottom: var(--space-4);
    font-size: 0.875rem;
    font-weight: 600;
    letter-spacing: 3.5px;
  }

  .product-feature__heading {
    margin-bottom: var(--space-6);
    color: var(--color-black);
  }

  .product-feature__body {
    color: var(--color-olive);
    line-height: var(--line-height-body);
    margin-bottom: var(--space-6);
  }

  .product-feature__body p {
    max-width: 52ch;
  }

  /* ---- Bullets ---- */
  .product-feature__bullets {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
    margin-bottom: var(--space-6);
  }

  .product-feature__bullet {
    display: flex;
    gap: var(--space-4);
    font-size: var(--font-size-sm);
    color: var(--color-olive);
    line-height: var(--line-height-body);
  }

  .product-feature__bullet-icon {
    color: var(--color-walnut);
    flex-shrink: 0;
    font-size: var(--font-size-md);
    line-height: 1.4;
  }

  /* ---- Responsive ---- */
  @media (max-width: 767px) {
    .product-feature__grid {
      grid-template-columns: 1fr;
      gap: var(--space-8);
    }

    .product-feature__grid--reverse .product-feature__media,
    .product-feature__grid--reverse .product-feature__copy {
      order: unset;
    }

    .product-feature__media {
      aspect-ratio: 4 / 3;
    }

    /* Videos showing a desk/surface scene are cropped upward by default.
       Shift object-position down so the desk (lower portion) is visible. */
    .product-feature__video {
      object-position: center 65%;
    }

    /* Extra bottom padding on mobile so the next section has breathing room */
    .product-feature.section {
      padding-bottom: 40px;
    }
  }
</style>

{% schema %}
{
  "name": "Product feature",
  "settings": [
    {
      "type": "text",
      "id": "video_url",
      "label": "Video URL (MP4)",
      "info": "Paste a hosted MP4 URL. Takes priority over image. Plays silently on loop."
    },
    {
      "type": "range",
      "id": "video_position_y",
      "label": "Video vertical crop position",
      "info": "0 = show top of video, 50 = centred (default), 100 = show bottom. Lower this if too much of the bottom of the frame is showing.",
      "min": 0,
      "max": 100,
      "step": 5,
      "default": 50,
      "unit": "%"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image",
      "info": "Used as poster (shown before/while video loads) when a video URL is set. Used as the visual when no video URL is set. Recommended size: 1200×1467 (tall portrait)."
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": "The science"
    },
    {
      "type": "textarea",
      "id": "heading",
      "label": "Heading",
      "default": "Most routines start too late."
    },
    {
      "type": "textarea",
      "id": "body",
      "label": "Body text",
      "default": "Most people don't know what their body is doing while they sleep. The Smart Band tracks HRV, recovery, and sleep architecture overnight, then tells you in the morning whether to push or rest."
    },
    {
      "type": "text",
      "id": "cta_label",
      "label": "Button label",
      "default": "Learn more"
    },
    {
      "type": "url",
      "id": "cta_url",
      "label": "Button URL"
    },
    {
      "type": "checkbox",
      "id": "reverse",
      "label": "Reverse layout (image on right)",
      "default": false
    }
  ],
  "blocks": [
    {
      "type": "bullet",
      "name": "Bullet point",
      "settings": [
        {
          "type": "text",
          "id": "text",
          "label": "Text",
          "default": "5–7 day battery. Wear it to sleep, every night."
        }
      ]
    }
  ],
  "presets": [{ "name": "Product feature" }]
}
{% endschema %}
